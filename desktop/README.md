# Desktop client moved

The G-Tunnel desktop client (Wails v3) now lives in its own private repository.

This directory is intentionally empty to reduce merge conflicts when syncing
with upstream. Contact the maintainer for access to the desktop repo.
